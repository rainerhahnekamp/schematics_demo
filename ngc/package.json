{
  "name": "@hahnekamp/ngc",
  "version": "0.0.7",
  "description": "A blank schematics",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && jasmine src/**/*_spec.js",
    "deploy": "npm run build && npm publish --registry http://localhost:4873/"
  },
  "keywords": [
    "schematics"
  ],
  "author": "",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "@angular-devkit/core": "^18.0.5",
    "@angular-devkit/schematics": "^18.0.5",
    "typescript": "~5.4.2"
  },
  "devDependencies": {
    "@types/node": "^18.18.0",
    "@types/jasmine": "~5.1.0",
    "jasmine": "^5.0.0"
  },
  "ng-update": {
    "migrations": "./src/migrations/migrations.json"
  }
}
